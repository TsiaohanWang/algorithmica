---
title: 子串查找
part: 字符串
weight: 30
---

考虑一个在你每次按 `ctrl+f` 时都会出现的问题：

> 有一大段文本 $t$。需要找出字符串 $s$ 在其中的所有出现位置。

把 $t$ 中所有长度为 $|s|$ 的子串都与 $s$ 比较的朴素解法，复杂度为 $O(|t| \cdot |s|)$。如果文本很大，在其中查找长单词就会变得非常慢。

然而，有很多方法可以在 $O(|s| + |t|)$ 内解决这个问题，其中两个最常见也最简单的方法是通过*前缀函数*和*z 函数*（*注意：不念「zi」，而念「zet*」）。
