---
title: 双端队列
weight: 7
draft: true
---

`deque` 是一种可以同时操作首尾两端的数据结构，也就是说支持在两侧进行插入和删除。

``` C++
deque<T> name; // дек типа T с названием name
name.front(), name.back(); // ссылка на первый и последний элемент соответственно
name.pop_front(), name.pop_back(); // удаление первого и последнего элемента
name.push_front(x), name.push_back(x); // вставка x в начало/конец
```

双端队列（deque）是一种数据结构，它以 $O(1)$ 的复杂度存储有序元素，并支持以下操作：

  - `push_back(x)` —— 把元素 x 放入队尾
  - `push_front(x)` —— 把元素 x 放入队首
  - `pop_back()` —— 从队尾取出并返回元素
  - `pop_front()` —— 从队首取出并返回元素

也就是说，队列和栈都可以用双端队列实现。大多数情况下，用双端队列代替队列反而更方便。

双端队列的一种实现方式是利用环形缓冲区。